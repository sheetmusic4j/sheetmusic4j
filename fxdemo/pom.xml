<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.sheetmusic4j</groupId>
        <artifactId>parent</artifactId>
        <version>0.0.2-SNAPSHOT</version>
    </parent>

    <artifactId>fxdemo</artifactId>
    <packaging>jar</packaging>

    <name>Sheetmusic4j :: FX Demo</name>
    <description>Standalone JavaFX demo application to test the fxviewer: load MusicXML/MIDI files, inspect the score,
        and view debug information.
    </description>

    <properties>
        <exec.mainClass>com.sheetmusic4j.fxdemo.SheetDemoApp</exec.mainClass>
        <!-- Demo application: inherits maven-deploy-plugin skip=true from the parent POM,
             so it is NOT published to Maven Central. -->
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.sheetmusic4j</groupId>
            <artifactId>core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.sheetmusic4j</groupId>
            <artifactId>engraving</artifactId>
        </dependency>
        <dependency>
            <groupId>com.sheetmusic4j</groupId>
            <artifactId>fxviewer</artifactId>
        </dependency>
        <dependency>
            <groupId>com.dlsc.pdfviewfx</groupId>
            <artifactId>pdfviewfx</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-swing</artifactId>
        </dependency>
        <!-- Log4j2 implementation for SLF4J (runtime logging backend for the demo app) -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j2-impl</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <configuration>
                    <mainClass>${exec.mainClass}</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Run the strip-view showcase: mvn -pl fxdemo -Pstrip javafx:run -->
        <profile>
            <id>strip</id>
            <properties>
                <exec.mainClass>com.sheetmusic4j.fxdemo.StripDemoApp</exec.mainClass>
            </properties>
        </profile>
    </profiles>
</project>
